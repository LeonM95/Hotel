@model IEnumerable<ProyectoHotel.Models.VENTA>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Total)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Estado)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RECEPCION.Observacion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Total)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Estado)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RECEPCION.Observacion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdVenta }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdVenta }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdVenta })
        </td>
    </tr>
}

</table>
